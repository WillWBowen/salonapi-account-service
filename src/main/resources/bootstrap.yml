spring:
  application:
    name: accountservice
  cloud:
    config:
      fail-fast: true
      username: user
  main:
    allow-bean-definition-overriding: true

management:
  endpoints:
    web:
      exposure:
        include: "*"